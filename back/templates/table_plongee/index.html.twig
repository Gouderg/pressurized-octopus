{% extends "base.html.twig" %}

{% set page_title = 'Tables plongée' %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/table_plongee.css') }}" type="text/css" rel="stylesheet" />
{% endblock %}

{% block body %}
    <div class="main">
        <div class="table-part">
            <table>
                <thead>
                    <tr> 
                        <th> ID </th>
                        <th> Nom </th>
                    </tr>
                    
                </thead>

                <tbody>
                    {% for table in tablePlongee %}
                        <tr>
                            <td>{{ table.id }}</td>
                            <td>{{ table.nom }}</td>
                            <td><a class="btn" href="{{ path('table_edit', {id: table.id} ) }}"> ✏️ </a></td>
                            <td><a class="btn" href="{{ path('table_delete', {id: table.id} ) }}"> 🗑️ </a></td>  
                        </tr>
                    {% endfor %}
                </tbody>
            </table>

            <a class="btn" href="{{ path('table_create') }}"> Ajouter une table de plongée </a>
        </div> 
    </div>
{% endblock %}
